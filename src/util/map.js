const mapHeight = 768;
const mapWidth = 1024;
const maxCoords = [-mapHeight, mapWidth];
// const yOffset = 51;

// export const mapCenter = [maxCoords[0] / 2 + yOffset, maxCoords[1] / 2];
export const mapCenter = [maxCoords[0] / 2, maxCoords[1] / 2];
export const mapBounds = [[0, 0], maxCoords];
